@{
    ViewData["Title"] = "Page 3";
}

@foreach (News item in (List<News>)ViewBag.News)
{
    <tr>
        <td>
            <h2>@item.Title</h2>
        </td>
        <td>
            <a href="@item.Link">Link to This new</a>

        </td>

    </tr>
}